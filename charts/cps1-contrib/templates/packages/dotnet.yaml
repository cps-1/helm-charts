{{- if has "dotnet" .Values.includePackages }}
apiVersion: cps1.tech/v1alpha2
kind: Package
metadata:
  name: dotnet
spec:
  label: Microsoft .NET
  icon:
    svg: |
      {{- .Files.Get "images/dotnet.svg" | nindent 6 }}
  properties:
    - name: version
      label: Version
      tip: .NET version
      kind: select
      required: true
      values: {{- toYaml .Values.packages.dotnet.version.values | nindent 8 }}
      default: "{{ .Values.packages.dotnet.version.default }}"
  setupScript: |
    {{- .Files.Get "scripts/packages/dotnet.sh" | nindent 4 }}
{{- end }}
